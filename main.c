#include "dwipe.h"
#include "defs.h"
#include "init.h"
#include "dwipe.h"
#include "stdint.h"
#include "disk.h"



void do_test(void)
{
    int line = LINE_SCROLL-2;
    int ret = 0;
    init();

    /*
    hprint(line+1, 0, (ulong)ret);

    hprint(line+2, 0, (ulong)&real_buffer);
    */



    while(1){
        
    }
}
